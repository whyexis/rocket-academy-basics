/**
 * ------------------------------------------------------------------------
 * Main
 * ------------------------------------------------------------------------
 */

function main(input) {
  var minutesInWeek = 10080;
  var output = minutesInWeek * input;
  var myOutputValue = `There are ${output} minutes in ${input} weeks! Wow!`;
  return myOutputValue;
}
